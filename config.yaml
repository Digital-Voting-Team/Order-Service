log:
  disable_sentry: true
  level: "info"

db:
  url: "postgres://postgres:postgres@db/postgres?sslmode=disable"
# Uncomment to use locally:
#   url: "postgres://postgres:password@localhost:5432/Order?sslmode=disable"
#   url: "postgres://postgres:postgres@localhost:5435/postgres?sslmode=disable"

listener:
  addr: :9102

cop:
  disabled: true
#  endpoint: "http://..."
#  upstream: "http://..."
#  service_name: order-service
#  service_port: 80
